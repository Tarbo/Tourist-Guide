<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:background">@color/background_colour</item>
    </style>

    <!-- TextView Styling for the App -->
    <style name="textview_style">
        <!-- Custom of the textViews in the app -->
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/text_color</item>
    </style>

    <style name="five_items_textviw_style">
        <!-- Custom of the textviews in the selection page -->
        <item name="android:textSize">35sp</item>
        <item name="android:scaleY">1.4</item>
        <item name="android:elevation">4dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>
</resources>
